<%= form_with(model: direcciones_tipo) do |form| %>
  <% if direcciones_tipo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(direcciones_tipo.errors.count, "error") %> prohibited this direcciones_tipo from being saved:</h2>

      <ul>
        <% direcciones_tipo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tipo, style: "display: block" %>
    <%= form.text_field :tipo %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
