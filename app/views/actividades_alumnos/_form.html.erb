<%= form_with(model: actividades_alumno) do |form| %>
  <% if actividades_alumno.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(actividades_alumno.errors.count, "error") %> prohibited this actividades_alumno from being saved:</h2>

      <ul>
        <% actividades_alumno.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :actividad_id, style: "display: block" %>
    
    <%= collection_select("actividades_alumno", :actividad_id, Actividad.all, :id, :tipo_id) %>
    <!--
   <%= form.number_field :actividad_id %>
    -->
  </div>

  <div>
    <%= form.label :alumno_id, style: "display: block" %>
    <%= collection_select("actividades_alumno", :alumno_id, Alumno.all, :id, :no_control) %>
    <!--
    <%= form.number_field :alumno_id %>
    -->
  </div>

  <div>
    <%= form.label :calificacion, style: "display: block" %>
    <%= form.text_field :calificacion %>
  </div>

  <div>
    <%= form.label :observaciones, style: "display: block" %>
    <%= form.text_area :observaciones %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
